# @title Get specific type of gene-disease association
# @param arg0=SIO_001120 
# @param arg1=10 

SELECT DISTINCT ?title ?dataset
WHERE {
    ?gda a sio:$1 ;
	 dct:identifier ?id ;
	 dct:title ?title ;
         rdfs:label ?label ;
	 rdfs:comment ?comment ;
	 void:inDataset ?dataset .
}
LIMIT $2

